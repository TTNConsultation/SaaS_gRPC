/*************************************************************
** Class generated by CodeTrigger, Version 6.3.0.4
** This class was generated on 2020-08-01 3:54:27 AM
** Changes to this file may cause incorrect behaviour and will be lost if the code is regenerated
**************************************************************/
using System;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using AAF.BusinessObjects;
using Expr = AAF.BusinessObjects;

namespace AAF.MVVMSample.ViewModel
{
	public partial class RestaurantLocationViewModel : IViewModelBase
	{
		/*WARNING - This class was generated by CodeTrigger. Changes to this file may cause incorrect behaviour and will be lost when the code is regenerated*/
		#region members
		private string _displayName = "RestaurantLocation";
		private readonly BORestaurantLocation _searchBO = new BORestaurantLocation();
		private readonly ObservableCollection<BORestaurantLocation> _boRestaurantLocations = new ObservableCollection<BORestaurantLocation>();
		private IList<BORestaurantLocation> _boRestaurantLocationsCached = new List<BORestaurantLocation>();
		private IList<BORestaurantLocation> _boRestaurantLocationsCachedDelete = new List<BORestaurantLocation>();
		private RelayCommand _deleteRowCommand;
		#endregion
		
		#region constructor
		public RestaurantLocationViewModel()
		{		}
		#endregion
		
		#region methods
		public static IList<BORestaurantLocation> GetRestaurantLocationList()
		{
			try
			{
				IList<BORestaurantLocation> listRestaurantLocation = BORestaurantLocation.RestaurantLocationCollection();
				return listRestaurantLocation;
			}
			catch(Exception)
			{	/*rethrow or handle gracefully*/return null;	}
			finally	{	}
		}
		
		private void AddExpr(ref Criteria<BORestaurantLocation> crit, string propertyName, object propertyValue, string propertyValueText, Func<object, string> formatter = null)
		{
			bool wildcard = propertyValueText.Contains("%");
			crit.Add(wildcard
				? Expression.Like(propertyName, propertyValue, formatter)
				: Expression.Eq(propertyName, propertyValue, formatter));
		}

		public int GetLoadCount()
		{
			var crit = new Criteria<BORestaurantLocation>();
			try
			{
				if(_searchBO.Id.HasValue) AddExpr(ref crit, "Id", _searchBO.Id, "" );
				if(_searchBO.RestaurantId.HasValue) AddExpr(ref crit, "RestaurantId", _searchBO.RestaurantId, "" );
				if(_searchBO.LocationId.HasValue) AddExpr(ref crit, "LocationId", _searchBO.LocationId, "" );
				if(!string.IsNullOrEmpty(_searchBO.Name)) AddExpr(ref crit, "Name", _searchBO.Name, _searchBO.Name);
				if(!string.IsNullOrEmpty(_searchBO.PhoneNumber)) AddExpr(ref crit, "PhoneNumber", _searchBO.PhoneNumber, _searchBO.PhoneNumber);
				if(_searchBO.Status.HasValue) AddExpr(ref crit, "Status", _searchBO.Status, "" );
				if(_searchBO.Since.HasValue) AddExpr(ref crit, "Since", _searchBO.Since, "" );
				int resultCount = crit.Count();
				return resultCount;
			}
			catch	{	throw;	}
			finally	{	}
		}
		
		public void LoadRecords()
		{
			var crit = new Criteria<BORestaurantLocation>();
			try
			{
				if(_searchBO.Id.HasValue) AddExpr(ref crit, "Id", _searchBO.Id, "" );
				if(_searchBO.RestaurantId.HasValue) AddExpr(ref crit, "RestaurantId", _searchBO.RestaurantId, "" );
				if(_searchBO.LocationId.HasValue) AddExpr(ref crit, "LocationId", _searchBO.LocationId, "" );
				if(!string.IsNullOrEmpty(_searchBO.Name)) AddExpr(ref crit, "Name", _searchBO.Name, _searchBO.Name);
				if(!string.IsNullOrEmpty(_searchBO.PhoneNumber)) AddExpr(ref crit, "PhoneNumber", _searchBO.PhoneNumber, _searchBO.PhoneNumber);
				if(_searchBO.Status.HasValue) AddExpr(ref crit, "Status", _searchBO.Status, "" );
				if(_searchBO.Since.HasValue) AddExpr(ref crit, "Since", _searchBO.Since, "" );
				
				_boRestaurantLocations.Clear();
				_boRestaurantLocationsCachedDelete.Clear();
				_boRestaurantLocationsCached = crit.List<BORestaurantLocation>();
				foreach(BORestaurantLocation boRestaurantLocation in _boRestaurantLocationsCached)
					_boRestaurantLocations.Add(boRestaurantLocation);
			}
			catch	{	throw;	}
			finally	{	}
		}
		
		public void SaveCurrentView()
		{
			try
			{
				foreach(BORestaurantLocation boRestaurantLocation in _boRestaurantLocations)
				{
					if(_boRestaurantLocationsCached.Contains(boRestaurantLocation))
					{
						if(!boRestaurantLocation.IsDirty) continue;
						try{ boRestaurantLocation.Update(); }
						catch(Exception ex)
						{ throw new Exception("Error updating record: CodeTrigger has detected a data exception. Possible invalid foreign key reference, missing fields or other error. Exception details follow below.\r\n\r\n" + ex.Message); }
					}
					else
					{
						try{ boRestaurantLocation.SaveNew(); }
						catch(Exception ex)
						{ throw new Exception("Error saving new record: CodeTrigger has detected a data exception. Possible duplicate primary key, invalid foreign key reference, or missing fields. Exception details follow below.\r\n\r\n" + ex.Message); }
					}
				}
				foreach(BORestaurantLocation boRestaurantLocation in _boRestaurantLocationsCachedDelete)
				{
					if((boRestaurantLocation == null)  || (!boRestaurantLocation.Id.HasValue))
					continue;
				
					try{ boRestaurantLocation.Delete(); }
					catch(Exception ex)
					{ throw new Exception("Error deleting record: CodeTrigger has detected a data exception. Possible existing foreign key reference or other error. Exception details follow below.\r\n\r\n" + ex.Message); }
				}
				_boRestaurantLocationsCachedDelete.Clear();
				
				LoadRecords();
			}
			catch	{	throw;	}
			finally	{	}
		}
		
		public void DeleteRow(object state)
		{
			if(state is BORestaurantLocation)
			{
				_boRestaurantLocations.Remove((BORestaurantLocation)state);
				_boRestaurantLocationsCachedDelete.Add((BORestaurantLocation)state);
			}
		}
		#endregion
		
		#region properties
		public BORestaurantLocation Filter
		{
			get {	return _searchBO;	}
		}
		public ObservableCollection<BORestaurantLocation> Results
		{
			get {	return _boRestaurantLocations;	}
		}
		
		public string DisplayName
		{
			get {	return _displayName;	}
			set {	_displayName = value;	}
		}
		
		public RelayCommand DeleteRowCommand
		{
			get
			{
				if(_deleteRowCommand == null)
					_deleteRowCommand = new RelayCommand(DeleteRow);
				return _deleteRowCommand;
			}
		}
		#endregion
	}
}

