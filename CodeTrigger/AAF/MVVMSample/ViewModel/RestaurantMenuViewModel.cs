/*************************************************************
** Class generated by CodeTrigger, Version 6.3.0.4
** This class was generated on 2020-08-01 3:54:27 AM
** Changes to this file may cause incorrect behaviour and will be lost if the code is regenerated
**************************************************************/
using System;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using AAF.BusinessObjects;
using Expr = AAF.BusinessObjects;

namespace AAF.MVVMSample.ViewModel
{
	public partial class RestaurantMenuViewModel : IViewModelBase
	{
		/*WARNING - This class was generated by CodeTrigger. Changes to this file may cause incorrect behaviour and will be lost when the code is regenerated*/
		#region members
		private string _displayName = "RestaurantMenu";
		private readonly BORestaurantMenu _searchBO = new BORestaurantMenu();
		private readonly ObservableCollection<BORestaurantMenu> _boRestaurantMenus = new ObservableCollection<BORestaurantMenu>();
		private IList<BORestaurantMenu> _boRestaurantMenusCached = new List<BORestaurantMenu>();
		private IList<BORestaurantMenu> _boRestaurantMenusCachedDelete = new List<BORestaurantMenu>();
		private RelayCommand _deleteRowCommand;
		#endregion
		
		#region constructor
		public RestaurantMenuViewModel()
		{		}
		#endregion
		
		#region methods
		public static IList<BORestaurantMenu> GetRestaurantMenuList()
		{
			try
			{
				IList<BORestaurantMenu> listRestaurantMenu = BORestaurantMenu.RestaurantMenuCollection();
				return listRestaurantMenu;
			}
			catch(Exception)
			{	/*rethrow or handle gracefully*/return null;	}
			finally	{	}
		}
		
		private void AddExpr(ref Criteria<BORestaurantMenu> crit, string propertyName, object propertyValue, string propertyValueText, Func<object, string> formatter = null)
		{
			bool wildcard = propertyValueText.Contains("%");
			crit.Add(wildcard
				? Expression.Like(propertyName, propertyValue, formatter)
				: Expression.Eq(propertyName, propertyValue, formatter));
		}

		public int GetLoadCount()
		{
			var crit = new Criteria<BORestaurantMenu>();
			try
			{
				if(_searchBO.Id.HasValue) AddExpr(ref crit, "Id", _searchBO.Id, "" );
				if(_searchBO.RestaurantId.HasValue) AddExpr(ref crit, "RestaurantId", _searchBO.RestaurantId, "" );
				if(_searchBO.StateId.HasValue) AddExpr(ref crit, "StateId", _searchBO.StateId, "" );
				int resultCount = crit.Count();
				return resultCount;
			}
			catch	{	throw;	}
			finally	{	}
		}
		
		public void LoadRecords()
		{
			var crit = new Criteria<BORestaurantMenu>();
			try
			{
				if(_searchBO.Id.HasValue) AddExpr(ref crit, "Id", _searchBO.Id, "" );
				if(_searchBO.RestaurantId.HasValue) AddExpr(ref crit, "RestaurantId", _searchBO.RestaurantId, "" );
				if(_searchBO.StateId.HasValue) AddExpr(ref crit, "StateId", _searchBO.StateId, "" );
				
				_boRestaurantMenus.Clear();
				_boRestaurantMenusCachedDelete.Clear();
				_boRestaurantMenusCached = crit.List<BORestaurantMenu>();
				foreach(BORestaurantMenu boRestaurantMenu in _boRestaurantMenusCached)
					_boRestaurantMenus.Add(boRestaurantMenu);
			}
			catch	{	throw;	}
			finally	{	}
		}
		
		public void SaveCurrentView()
		{
			try
			{
				foreach(BORestaurantMenu boRestaurantMenu in _boRestaurantMenus)
				{
					if(_boRestaurantMenusCached.Contains(boRestaurantMenu))
					{
						if(!boRestaurantMenu.IsDirty) continue;
						try{ boRestaurantMenu.Update(); }
						catch(Exception ex)
						{ throw new Exception("Error updating record: CodeTrigger has detected a data exception. Possible invalid foreign key reference, missing fields or other error. Exception details follow below.\r\n\r\n" + ex.Message); }
					}
					else
					{
						try{ boRestaurantMenu.SaveNew(); }
						catch(Exception ex)
						{ throw new Exception("Error saving new record: CodeTrigger has detected a data exception. Possible duplicate primary key, invalid foreign key reference, or missing fields. Exception details follow below.\r\n\r\n" + ex.Message); }
					}
				}
				foreach(BORestaurantMenu boRestaurantMenu in _boRestaurantMenusCachedDelete)
				{
					if((boRestaurantMenu == null)  || (!boRestaurantMenu.Id.HasValue))
					continue;
				
					try{ boRestaurantMenu.Delete(); }
					catch(Exception ex)
					{ throw new Exception("Error deleting record: CodeTrigger has detected a data exception. Possible existing foreign key reference or other error. Exception details follow below.\r\n\r\n" + ex.Message); }
				}
				_boRestaurantMenusCachedDelete.Clear();
				
				LoadRecords();
			}
			catch	{	throw;	}
			finally	{	}
		}
		
		public void DeleteRow(object state)
		{
			if(state is BORestaurantMenu)
			{
				_boRestaurantMenus.Remove((BORestaurantMenu)state);
				_boRestaurantMenusCachedDelete.Add((BORestaurantMenu)state);
			}
		}
		#endregion
		
		#region properties
		public BORestaurantMenu Filter
		{
			get {	return _searchBO;	}
		}
		public ObservableCollection<BORestaurantMenu> Results
		{
			get {	return _boRestaurantMenus;	}
		}
		
		public string DisplayName
		{
			get {	return _displayName;	}
			set {	_displayName = value;	}
		}
		
		public RelayCommand DeleteRowCommand
		{
			get
			{
				if(_deleteRowCommand == null)
					_deleteRowCommand = new RelayCommand(DeleteRow);
				return _deleteRowCommand;
			}
		}
		#endregion
	}
}

